class Settings {
    static randomColors = false;
    static showNumbers = true;
    static showAllNumbers = true;
    static speed = 50; // `CollatzNumber.next()` calls per second
    static slideSpeed = 1; // higher = slower
}